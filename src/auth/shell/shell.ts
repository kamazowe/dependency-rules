import { featureA } from '../features/feature-a/feature-a';
import { featureB } from '../features/feature-b/feature-b';
import { utilX } from '../utils/util-x/util-x';
import { domain } from '../domain/domain';

export const shell = '[auth] shell' + featureA + featureB + utilX + domain
