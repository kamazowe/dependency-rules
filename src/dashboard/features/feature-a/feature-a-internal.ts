import { dashboardUiA } from '../../ui/ui-a/ui-a';
import { dashboardDomain } from '../../domain/domain';

export const featureAInternal = '[dashboard] feature A internal' + dashboardUiA + dashboardDomain;
