import { dashboardFeatureA } from '../features/feature-a/feature-a';
import { featureB } from '../features/feature-b/feature-b';
import { dashboardUtilX } from '../utils/util-x/util-x';
import { dashboardDomain } from '../domain/domain';

export const dashboardShell = '[dashboard] shell' + dashboardFeatureA + featureB + dashboardUtilX + dashboardDomain
